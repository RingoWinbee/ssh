package com.ringo.ssh.dao;

import org.junit.Test;
public class IUserDaoTest extends BaseTestCaseJunit44 {
	@Test
	public void testInit() {
		//不用写东西，启动hibernate时会自动检查数据库，如果缺少表，则自动建表；如果表里缺少列，则自动添加列
		}
}
